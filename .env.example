# Database (SQLite by default — zero external dependencies)
DATABASE_URL="file:./dev.db"

# Apollo.io integration (cash-buyer discovery & enrichment) — optional
APOLLO_API_KEY=""

# Session signing secret — set a long random string in production
SESSION_SECRET="change-me-to-a-long-random-string"

# App
NEXT_PUBLIC_APP_NAME="WholesaleOS"
